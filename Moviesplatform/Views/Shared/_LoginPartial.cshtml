@using Microsoft.AspNetCore.Identity
@using Moviesplatform.Areas.Identity.Data

@inject SignInManager<MoviesplatformUser> SignInManager
@inject UserManager<MoviesplatformUser> UserManager

<ul class="navbar-nav">
@if (SignInManager.IsSignedIn(User))
{
    <li class="nav-item">
            <a id="manage" style="margin-right:70px !important;
    color:blue!important; font-size:larger !important;font-weight:bolder !important" class="nav-link text-dark" asp-area="Identity" asp-page="/Account/Manage/Index" title="Manage">Hello @UserManager.GetUserName(User)!</a>
    </li>
    <li class="nav-item">
      @*   <form id="logoutForm" class="form-inline" asp-area="Identity" asp-page="/Account/Logout" asp-route-returnUrl="@Url.Action("Index", "Home", new { area = "" })">
                <button id="logout" type="submit" class="nav-link btn btn-link text-dark border-0" style="margin-right:70px !important;
    color:red !important; font-size:larger !important;font-weight:bolder !important">
                    Logout
                </button>
        </form> *@

            <form asp-area="Identity" asp-page="/Account/Logout" method="post" id="logoutForm">
                <button type="submit" class="nav-link btn btn-link text-dark" style="margin-right:70px !important;
    color:red !important; font-size:larger !important;font-weight:bolder !important">
                    Logout
                </button>
            </form>
    </li>
}
else
{
    <li class="nav-item">
            <a class="nav-link text-dark " id="register" asp-area="Identity" asp-page="/Account/Register" style="margin-right:80px !important;
    color:blue!important; font-size:larger !important;font-weight:bolder !important">Register</a>

    </li>
    <li class="nav-item">
            <a class="nav-link text-dark" id="login" asp-area="Identity" asp-page="/Account/Login" style="margin-right:70px !important;
    color:blue!important; font-size:larger !important;font-weight:bolder !important">Login</a>
    </li>
}
</ul>
